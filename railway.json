{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "pip install -r backend/requirements.txt && npm install"
    },
    "start": "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080"
  }