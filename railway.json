{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "pip install --upgrade pip --user && pip install -r backend/requirements.txt --user && npm install"
    },
    "start": "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080"
  }