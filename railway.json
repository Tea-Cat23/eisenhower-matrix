{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "pip install --break-system-packages --upgrade pip && pip install --break-system-packages uvicorn fastapi -r backend/requirements.txt && npm install"
    },
    "preDeployCommand": "pip install --break-system-packages --upgrade pip && pip install --break-system-packages uvicorn fastapi -r backend/requirements.txt",
    "start": "/root/.nix-profile/bin/python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080"
}