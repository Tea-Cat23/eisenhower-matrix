{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "python3 -m venv /app/venv && /app/venv/bin/pip install --upgrade pip && /app/venv/bin/pip install uvicorn fastapi -r backend/requirements.txt && npm install"
    },
    "start": "/app/venv/bin/python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080"
}