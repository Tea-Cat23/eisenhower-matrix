{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "pip install --upgrade pip && pip install --break-system-packages uvicorn fastapi -r backend/requirements.txt && npm install"
    },
    "start": "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080"
  }