{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "nix-env -iA nixpkgs.nodejs nixpkgs.python311 nixpkgs.pip && npm install -g npm && npm ci && pip install -r backend/requirements.txt"
  },
  "start": "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080"
}